# 프로젝트의 목적
링크를 통해 사람들에게 공유 가능한 모바일 청첩장 제작

## 사용 기술
* FrontEnd만 구현 후 GitHub Hosting으로 서빙
* FrontEnd는 React로 구현하는 것을 선호

## 구성
단일 페이지로 길게 구성하며, 스크롤로 내리는 형태로 구현
위에서부터 아래로 스크롤 내리는 순서대로 아래에서 설명.
1. 결혼 날짜, 시간, 장소를 간단하게 글자로만 표기하며 간단한 작은 달력도 표시해주면 좋음
2. 초대 문구. 4~5줄의 초대 문구와 함께 그 밑에는 OOO, OOO의 장남 OOO 등의 문구 들어감
3. 결혼 사진이 들어감. 한 줄로 작게 긴 형식으로 일단 보여주고, 하나를 클릭했을 때 확대되며, 확대된 UI에서 좌우로 넘겨서 전/후 사진으로 넘어갈 수 있게끔 구현
4. "오시는 길"을 네이버 지도와 연동하여 보여줌. 자차 이용, 대중교통 이용시 어떻게 와야하는지에 대한 설명도 필요하므로 잘 구획을 나눠서 구현
5. 간단하게 댓글 남길 수 있는 공간 구현. 이름과 내용만 적어 남길 수 있으며, 등록 후 삭제/수정은 안됨
6. 결혼식에 참석 못 하시는 분들을 위해 계좌를 표기 (클립보드 복사 기능 구현)
7. 카카오톡으로 공유하기, 링크 복사하기 버튼 구현
8. 배경음악 재생 및 음소거 버튼 (우상단 고정)

## 배경음악 추가 방법

1. 음악 파일(예: `bgm.mp3`)을 **`public/` 폴더**에 넣는다.
2. 파일 이름이 `bgm.mp3`가 아닐 경우 `src/components/BackgroundMusic/index.tsx`의 `MUSIC_SRC` 상수를 수정한다.
   ```typescript
   const MUSIC_SRC = `${import.meta.env.BASE_URL}파일명.mp3`;
   ```
3. 지원 포맷: `.mp3`, `.ogg`, `.wav` 등 브라우저 기본 지원 오디오 형식

## 구현 전 참고사항
* 수정을 하기전에 항상 TODO 리스트를 먼저 세우고, 이를 TODO.md로 남김. 이 파일을 커밋할 필요는 없음.
* 수정을 하기전에 결정이 필요한 것, 애매한 부분이 있으면 개발자에게 확인 후 진행
* 웹 상에 이미 잘 구현된 모바일 청첩장 참고할 것